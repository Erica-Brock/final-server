import {NextFunction, Response, Request} from 'express';
import { row, rows, empty } from './base.proc';
    const create = ()=>{};
    const read = ()=>{};
    const update = ()=>{};
    const destroy = ()=>{};
    const all = ()=>{};

export const routes = {
    create,
    read,
    update,
    destroy,
    all,
}